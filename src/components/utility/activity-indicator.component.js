import React from "react";
import { ActivityIndicator, Colors } from "react-native-paper";

const AppActivityIndicator = () => {
  return <ActivityIndicator animating={true} color={Colors.blue300} />;
};

export default AppActivityIndicator;
